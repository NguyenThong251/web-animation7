# web-animation7

Faithful 1:1 clone of **https://www.andreucci.fr/** — a WordPress site (custom theme) for Andreucci pizzeria, Mulhouse. GSAP + ScrollTrigger + Locomotive Scroll.

- Runs locally on port 3600 (`npm run dev`).
- 8 pages; all same-origin assets (CSS/fonts/images) downloaded + rewritten local; JS libs on CDN.
